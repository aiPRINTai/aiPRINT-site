# --- REQUIRED ---
# Stripe live (or test) secret key from Dashboard → Developers → API keys
STRIPE_SECRET_KEY=sk_live_xxx

# Where your site lives (the pages that have index.html/success.html)
# e.g. during dev: http://localhost:5173   in prod: https://aiprint.ai
CLIENT_URL=http://localhost:5173

# Port for this server (change if you like)
PORT=3000

# --- OPTIONAL (webhooks) ---
# When you run `npx stripe listen --forward-to localhost:3000/webhook`,
# it prints a secret like whsec_abc... Put it here (dev) or set it in prod.
STRIPE_WEBHOOK_SECRET=
